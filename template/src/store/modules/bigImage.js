// initial state
const state = {
  show: false,
  src: ''
}

// getters
const getters = {}

// actions
const actions = {}

// mutations
const mutations = {
  setBigImage(state, data) {
    Object.assign(state, data)
  }
}

export default {
  state,
  getters,
  actions,
  mutations
}
